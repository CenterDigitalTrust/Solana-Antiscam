CREATE TABLE IF NOT EXISTS public.daily_stats (id INT PRIMARY KEY DEFAULT 1, scanned_today INT DEFAULT 0, rejected_today INT DEFAULT 0, passed_today INT DEFAULT 0, daily_pnl_usd NUMERIC DEFAULT 0, updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL); ALTER TABLE public.daily_stats ENABLE ROW LEVEL SECURITY; CREATE POLICY "Public profiles are viewable by everyone." ON public.daily_stats FOR SELECT USING ( true ); ALTER TABLE public.tokens ADD COLUMN IF NOT EXISTS initial_price_usd NUMERIC DEFAULT 0; alter publication supabase_realtime add table public.daily_stats; INSERT INTO public.daily_stats (id, scanned_today, rejected_today, passed_today, daily_pnl_usd) VALUES (1, 4812, 4398, 414, 12.80) ON CONFLICT (id) DO NOTHING;